<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <!--Replace with much more compact code further below
  <% @users.each do |user| %>
		<%= render user %>   
  <% end %>
  !-->
  <%= render @users %>
</ul>

<%= will_paginate %>